# 📈 NE Data Extractor

Этот инструмент предназначен для извлечения ключевой информации о сетевом оборудовании (NE) из ZIP-архивов, содержащих файлы логов `NEData_*.txt`, и сохранения её в удобном формате XLSX. Он позволяет быстро получить основные параметры NE для анализа и отчетности.

## ✨ Возможности

*   Извлечение файлов `NEData_*.txt` из ZIP-арархивов.
*   Парсинг основной конфигурационной информации NE из блока `:cfg-nepara:`.
*   Сохранение извлеченных данных в формате `.xlsx`.
*   Генерация исполняемого файла (`.exe`) для запуска на любой машине без установки Python.
*   Логирование операций в отдельный файл для отслеживания процесса и отладки.
*   Отображение информации об организации при запуске.

## 📁 Структура Проекта

```
.
├── Input/
│   └── ваш_архив.zip             # Место для ваших ZIP-архивов с данными NE
├── Output/
│   └── ваш_архив_timestamp.xlsx  # Сюда будут сохраняться выходные XLSX-файлы
├── logs/
│   └── script_log.log            # Файл для логирования работы скрипта
├── main.py                       # Главный исполняемый скрипт
├── config.py                     # Конфигурационные параметры (пути, имена колонок)
├── data_parser.py                # Логика парсинга данных из NEData файлов
├── file_processor.py             # Обработка ZIP-архивов и извлечение текстовых файлов
├── excel_writer.py               # Запись данных в XLSX-файл
├── logger_config.py              # Настройка системы логирования
├── organization_info.txt         # Информация об организации
├── icon.ico (опционально)        # Файл иконки для исполняемого файла
├── NE_Info_Extractor.exe         # Скомпилированный исполняемый файл (после сборки)
├── main.spec                     # Файл спецификации PyInstaller (после первой сборки)
└── README.md                     # Этот файл документации
```

## 🚀 Быстрый старт

### 1. Подготовка

1.  **Клонируйте репозиторий** (или создайте все файлы вручную).
2.  **Создайте папки:** В корневом каталоге проекта создайте пустые папки `Input`, `Output` и `logs`.
3.  **Поместите ZIP-архивы:** Скопируйте ваши ZIP-архивы, содержащие файлы `NEData_*.txt`, в папку `Input`.
4.  **Файл `organization_info.txt`:** Создайте файл `organization_info.txt` в корне проекта и заполните его информацией об организации.

    Пример `organization_info.txt`:
    ```
    Название организации: Моя Компания
    Отдел: Отдел Автоматизации
    Контактное лицо: Иван Иванов
    Версия скрипта: 2.0
    Дата создания: 2023-11-20
    ```

### 2. Запуск

#### А. Запуск из исходного кода (требуется Python)

1.  **Установите зависимости:**
    ```bash
    pip install pandas openpyxl
    ```
2.  **Запустите главный скрипт:**
    ```bash
    python main.py
    ```

#### Б. Запуск исполняемого файла (.exe)

1.  Перейдите в папку `dist` (она появится после сборки, см. раздел "Сборка в .exe").
2.  Запустите файл `NE_Info_Extractor.exe`.

---

После запуска приложение будет искать ZIP-архивы в папке `Input`, извлекать из них соответствующие файлы, парсить данные и сохранять результат в папке `Output`. Все операции будут логироваться в файл `logs/script_log.log`.

## ⚙️ Конфигурация

Все основные настройки находятся в файле `config.py`:

*   `INPUT_DIR`: Директория для входных ZIP-архивов.
*   `OUTPUT_DIR`: Директория для выходных XLSX-файлов.
*   `NE_DATA_FILE_PREFIX`: Префикс для файлов, которые нужно парсить (по умолчанию "NEData").
*   `EXCEL_COLUMNS`: Список названий столбцов, которые будут использоваться в итоговом XLSX-файле.

Вы можете изменить их напрямую в `config.py` или, в будущем, через графический интерфейс.

## 📊 Извлекаемые данные

Скрипт извлекает следующие поля из блока `:cfg-nepara:`:

*   **NE Name**: Имя сетевого элемента.
*   **NE ID**: Идентификатор сетевого элемента.
*   **Creator**: Создатель конфигурации.
*   **Buildtime**: Время сборки конфигурации.
*   **Device**: Тип устройства.
*   **vrc_ver**: Версия VRC.
*   **ip**: IP-адрес устройства.

## 📝 Логирование

Инструмент использует модуль `logging` для записи всех важных событий в файл `logs/script_log.log`. В консоль выводятся сообщения уровня `INFO` и выше, а в лог-файл — все сообщения уровня `INFO` и выше. Это позволяет отслеживать процесс выполнения и выявлять потенциальные проблемы.

## 📦 Сборка в .exe (PyInstaller)

Для создания самодостаточного исполняемого файла, который можно запускать на Windows без установки Python, используется `PyInstaller`.

### Требования:

*   `PyInstaller`: `pip install pyinstaller`

### Шаги по сборке:

1.  Убедитесь, что все файлы проекта находятся в одной директории.
2.  (Опционально) Создайте файл иконки (`.ico`) и поместите его в корень проекта, например, `icon.ico`.
3.  Откройте терминал/командную строку в корневом каталоге проекта.
4.  Выполните команду сборки:

    ```bash
    pyinstaller --onefile --console --icon=icon.ico --add-data "organization_info.txt;." --add-data "config.py;." --add-data "data_parser.py;." --add-data "file_processor.py;." --add-data "excel_writer.py;." --add-data "logger_config.py;." main.py
    ```

    *   `--onefile`: Создает один исполняемый файл.
    *   `--console`: Запускает приложение с консольным окном.
    *   `--icon=icon.ico`: Присваивает указанную иконку файлу `.exe`. Замените `icon.ico` на имя вашего файла.
    *   `--add-data "source;dest"`: Включает дополнительные файлы и модули в сборку. `.` в `dest` означает корневой каталог внутри создаваемого `exe`.

5.  После успешной сборки, исполняемый файл `NE_Info_Extractor.exe` (или `main.exe`) будет находиться в папке `dist/` в корне проекта.

### Использование .spec файла (для более сложных сборок)

Если команда `pyinstaller` становится слишком длинной, вы можете отредактировать файл `.spec`, который генерируется после первого запуска `pyinstaller main.py`.

1.  Создайте `.spec` файл:
    ```bash
    pyinstaller main.py
    ```
2.  Отредактируйте `main.spec` (добавьте `datas`, `icon`, `name` и т.д. как показано в примерах).
3.  Выполните сборку с использованием `.spec` файла:
    ```bash
    pyinstaller main.spec
    ```

## ⚠️ Важные примечания

*   Для корректной работы `exe` файла, папки `Input`, `Output` и `logs` должны находиться в той же директории, что и `NE_Info_Extractor.exe`.
*   При возникновении ошибок, всегда проверяйте файл `logs/script_log.log` для получения детальной информации.

---

Разработано [Agafonov Oleg]
Версия: 1.0.2
```